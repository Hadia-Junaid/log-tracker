export interface ApplicationData {
    _id: string;
    name: string;
    hostname: string;
    environment: string;
    description: string;
    createdAt: string;
    updatedAt: string;
    createdAgo: string;
}

